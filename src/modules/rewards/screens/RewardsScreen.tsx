import React from "react";
import { Text, StyleSheet } from "react-native";

const RewardScreen = () => {
  return <Text style={styles.text}>Reward</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default RewardScreen;
