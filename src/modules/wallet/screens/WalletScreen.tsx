import React from "react";
import { Text, StyleSheet } from "react-native";

const WalletScreen = () => {
  return <Text style={styles.text}>Wallet</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default WalletScreen;
