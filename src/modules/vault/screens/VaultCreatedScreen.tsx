import React from "react";
import { Text, StyleSheet } from "react-native";

const VaultCreatedScreen = () => {
  return <Text style={styles.text}>VaultCreated</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default VaultCreatedScreen;
