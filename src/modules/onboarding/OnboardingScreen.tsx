import React from 'react';
import { Text, StyleSheet } from 'react-native';

const OnboardingScreen = () => {
  return <Text style={styles.text}>Onboarding</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default OnboardingScreen;
